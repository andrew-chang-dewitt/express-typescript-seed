{
  "name": "express-typescript-sample",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "prettier --config .prettierrc.yaml --write .",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "dev": "tsc-watch -p tsconfig.json --noClear --onSuccess \"node .\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "morgan": "^1.10.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/morgan": "^1.9.0",
    "@types/node": "^13.11.1",
    "prettier": "^2.0.4",
    "tsc-watch": "^4.2.3",
    "typescript": "^3.8.3"
  }
}
